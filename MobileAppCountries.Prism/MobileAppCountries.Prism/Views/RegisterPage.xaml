<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="MobileAppCountries.Prism.Views.RegisterPage">

    <StackLayout>
        <StackLayout Orientation="Vertical">
            <Label>User Name</Label>
            <Entry Text="{Binding UserName}"></Entry>
        </StackLayout>
        <StackLayout Orientation="Vertical">
            <Label>Email</Label>
            <Entry Text="{Binding Email}"></Entry>
        </StackLayout>
        <StackLayout Orientation="Vertical">
            <Label>Password</Label>
            <Entry Text="{Binding Password}"></Entry>
        </StackLayout>
        <StackLayout Orientation="Vertical">
            <Label>Confirm</Label>
            <Entry Text="{Binding Confirm}"></Entry>
        </StackLayout>
        <StackLayout Orientation="Vertical">
            <Label>Status</Label>
            <Entry Text="{Binding Status}"></Entry>
        </StackLayout>
        <Button Command="{Binding RegisterNewUser}"></Button>
    </StackLayout>
</ContentPage>